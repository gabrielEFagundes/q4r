// returns integer for exit codes, this is 
// a pattern you'll observe across most of the
// examples here.

// YET TODO
f main() int{
    //int value = 5
    *int p = 10

    // *p = 10
    // return value // 10 if deref-write reached, 5 if it didn't
}